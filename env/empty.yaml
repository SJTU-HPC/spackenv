---
spack_bin_root:         ~/spack
spack_install_root:     ~/spack/opt
spack_modules_root:     ~/spack/share/spack/modules/linux-centos7-x86_64/

modules:
  tcl:
    hash_length: 0
    naming_scheme: '{name}/{version}-{compiler.name}-{compiler.version}'
    all:
      conflict:
        - '{name}' 

compilers:
  - compiler:
      environment: {}
      extra_rpaths: []
      flags: {}
      modules: []
      operating_system: centos7
      paths:
        cc: /usr/bin/gcc
        cxx: /usr/bin/g++
        f77: /usr/bin/gfortran
        fc: /usr/bin/gfortran
      spec: gcc@4.8.5
      target: x86_64

packages: []

install: []

mirrors:
  remote_server: http://spack.pi.sjtu.edu.cn/mirror
